{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-md btn-primary btn-block" type="submit">{% trans %}Submit{% endtrans %}</button>
    {{ form_end(form) }}
{% endblock %}

