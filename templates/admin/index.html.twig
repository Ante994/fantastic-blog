{% extends 'base.html.twig' %}

{% block body %}
        <h1>Admin tools</h1>
        <p> Post
            <a href="{{ path('post_index')}}" class="pull-right btn btn-link">
                <span class="fas fa-pencil-alt">Show posts</span>
            </a>
            <a href="{{ path('post_new')}}" class="pull-right btn btn-link">
                <span class="fas fa-pencil-alt">Create post</span>
            </a>
            <a href="{{ path('post_index')}}" class="pull-right btn btn-link">
                <span class="fas fa-pencil-alt">Filter</span>
            </a>
        </p>
        <p>Tag
            <a href="{{ path('post_index')}}" class="pull-right btn btn-link">
                <span class="fas fa-pencil-alt">Show tags</span>
            </a>
            <a href="{{ path('post_new')}}" class="pull-right btn btn-link">
                <span class="fas fa-pencil-alt">Create tag</span>
            </a>
        </p>
        <p>Users
            <a href="{{ path('post_index')}}" class="pull-right btn btn-link">
                <span class="fas fa-pencil-alt">Show users</span>
            </a>
        </p>

        <p>Statistics
            <span>Likes</span>
            <span>Comments</span>
            <span>Favorite</span>
        </p>


{% endblock %}