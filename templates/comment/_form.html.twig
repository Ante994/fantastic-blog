{{ form_start(form) }}
    {{ form_errors(form.content) }}
    {{ form_widget(form.content) }}
    <button class="comment-submit" class="btn">{{ button_label|default('Comment') }}</button>
{{ form_end(form) }}
